# Mod開発前セットアップレビュー

**作成日**: 2025年12月16日  
**プロジェクト**: KuRadialMenu  
**ステータス**: 🔴 重大な問題あり - 対応必須

---

## 📋 概要

KuRadialMenu プロジェクトの開発前準備状況を、Fabric Mod 開発のベストプラクティスと照らし合わせて調査しました。プロジェクトは Stonecutter による複数バージョン対応を採用しており、基本的なディレクトリ構造は整っていますが、**開発を開始する前に修正が必要な重大な問題が複数存在**します。

---

## 🔴 重大な問題（即時対応必須）

### 1. **Java バージョンの不一致 - ビルド不可能状態**

**問題**: 現在の Java 8 では Fabric Loom 1.13+ がビルドできない

```
実行環境: Java 8 (1.8.0_461)
必要環境: Java 21 (Fabric Loom 1.13.6 の要件)
```

**影響**: 
- Gradle タスクが一切実行できない（ビルド、テスト、実行すべて不可）
- IDE のコード補完や型チェックが機能しない可能性
- 1.21.1, 1.21.11 のビルドは Java 21 必須

**修正方法**:
1. Java 21 をインストール（AdoptiumやAmazon Corretto推奨）
2. プロジェクトルートに `.java-version` ファイルを作成（バージョン管理ツール用）
3. `JAVA_HOME` 環境変数を設定
4. IntelliJ IDEA のプロジェクト SDK を Java 21 に設定

---

### 2. **Mixin クラスの重大な誤り - Client Mod なのに Server クラスに注入**

**問題**: `KuRadialMenuMixin.java` が `MinecraftServer` クラスに注入を試みている

```java
@Mixin(MinecraftServer.class)  // ❌ Client Mod が Server クラスをターゲット
public class KuRadialMenuMixin {
    @Inject(method = "loadLevel", at = @At("HEAD"))
    private void init(CallbackInfo info) { }
}
```

**影響**:
- Client 環境で `MinecraftServer` クラスが存在せず、実行時にクラッシュする可能性
- `fabric.mod.json` で `"environment": "client"` と宣言しているにもかかわらず矛盾
- Mixin 設定ファイル名が `kuradialmenu.mixins.json` だが、パッケージは `client` 専用

**修正方法**:
1. **即座に削除**するか、Client 専用クラス（例: `MinecraftClient`, `Screen`, `KeyboardHandler` など）をターゲットに変更
2. Mixin 設定ファイルに `client` 専用であることを明示:
   ```json
   {
     "required": true,
     "package": "dev.kurowater.kuradialmenu.mixin.client",
     "compatibilityLevel": "${java}",
     "client": [  // ← "mixins" ではなく "client"
       "ExampleClientMixin"
     ],
     "injectors": {
       "defaultRequire": 1
     }
   }
   ```

---

### 3. **Access Widener が有効化されていない**

**問題**: `kuradialmenu.accesswidener` ファイルは存在するが、設定が無効化されている

```kotlin
// build.gradle.kts (50行目)
// accessWidenerPath = rootProject.file("src/main/resources/template.accesswidener")
```

また、`fabric.mod.json` にも `accessWidener` フィールドが未定義。

**影響**:
- Private フィールドやメソッドへのアクセスが必要な場合に対応できない
- ファイルだけ存在しても機能しない

**修正方法**:
1. `build.gradle.kts` のコメントアウトを解除し、パスを修正:
   ```kotlin
   accessWidenerPath = rootProject.file("src/main/resources/kuradialmenu.accesswidener")
   ```
2. `fabric.mod.json` に追加:
   ```json
   {
     "accessWidener": "kuradialmenu.accesswidener"
   }
   ```
3. 現時点で Access Widener が不要なら、ファイルを削除

---

## 🟡 推奨される改善事項

### 4. **言語ファイルの欠落**

**問題**: `src/main/resources/assets/kuradialmenu/lang/` ディレクトリが存在しない

**影響**:
- UI に表示されるテキストがすべて翻訳キー（例: `key.kuradialmenu.open_menu`）のまま表示される
- 国際化（i18n）対応ができない

**推奨対応**:
最低限 `en_us.json` と `ja_jp.json` を作成

```
src/main/resources/assets/kuradialmenu/lang/
├── en_us.json
└── ja_jp.json
```

**サンプル** (`en_us.json`):
```json
{
  "key.category.kuradialmenu": "KuRadialMenu",
  "key.kuradialmenu.open_menu": "Open Radial Menu"
}
```

---

### 5. **ディレクトリ構造の不明瞭さ**

**問題**: 以下のディレクトリが存在するが、ファイルが1つもない

```
src/main/java/dev/kurowater/kuradialmenu/
├── client/
│   ├── config/
│   ├── keybind/
│   └── ui/
├── common/
│   ├── model/
│   └── util/
└── mixin/
```

**影響**:
- 空のディレクトリは Git で追跡されないため、開発環境によって構造が不一致になる
- プロジェクト構造が不明確で、新規参加者が混乱する

**推奨対応**:
1. 各ディレクトリに `.gitkeep` ファイルを配置（Git 管理下に置くため）
2. または、README.md でディレクトリ構成の意図を文書化
3. 実装開始時に、各ディレクトリの責務を明確にしたクラスを配置

---

### 6. **CHANGELOG.md が空**

**問題**: `CHANGELOG.md` ファイルが存在するが内容が空

**影響**:
- バージョン管理ができない
- ユーザーが変更内容を把握できない
- リリース時に変更履歴が追跡できない

**推奨対応**:
[Keep a Changelog](https://keepachangelog.com/) 形式を採用

```markdown
# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- Initial project setup with Stonecutter multi-version support
- Basic Fabric Mod structure for 1.19.4, 1.20.1, 1.21.1, 1.21.11

### Changed

### Fixed

## [0.1.0] - YYYY-MM-DD

### Added
- Project initialized
```

---

### 7. **README.md がテンプレートのまま**

**問題**: README.md が Stonecutter のテンプレート内容で、プロジェクト固有の情報がない

**推奨対応**:
以下のセクションを含むプロジェクト専用 README に更新

- プロジェクトの目的・機能概要
- インストール方法
- 使用方法（キーバインド等）
- 開発環境のセットアップ手順
- コントリビューション方法
- ライセンス情報

---

### 8. **エディタ設定の欠落 (.editorconfig)**

**問題**: `.editorconfig` ファイルが存在しない

**影響**:
- 開発者間でインデントスタイル、改行コード、文字エンコーディングが不統一になる
- コードレビュー時に無駄な差分が発生

**推奨対応**:
以下の `.editorconfig` を作成

```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

[*.java]
indent_style = space
indent_size = 4

[*.{json,yml,yaml}]
indent_style = space
indent_size = 2

[*.md]
trim_trailing_whitespace = false

[*.bat]
end_of_line = crlf
```

---

### 9. **CI/CD パイプラインの欠落**

**問題**: `.github/workflows/` ディレクトリが存在しない

**影響**:
- プルリクエスト時の自動ビルド・テストができない
- リリース作業が手動で煩雑
- コード品質の自動チェックができない

**推奨対応**:
最低限以下のワークフローを作成

1. **ビルド検証** (`build.yml`): PR/Push 時に全バージョンビルド
2. **リリース自動化** (`release.yml`): タグ push 時に自動リリース
3. **依存関係更新** (`dependabot.yml`): Gradle/Fabric API の自動更新

---

### 10. **テストコードの完全欠落**

**問題**: `src/test/` ディレクトリが存在せず、単体テストが皆無

**影響**:
- バグの早期発見ができない
- リファクタリング時の回帰テストができない
- コードの品質保証ができない

**推奨対応**:
以下の構造でテスト環境を整備

```
src/
├── main/
└── test/
    └── java/
        └── dev/
            └── kurowater/
                └── kuradialmenu/
                    └── KuRadialMenuClientTest.java
```

Fabric では `fabric-gametest-api-v1` を使用したゲーム内テストも検討。

---

## 🟢 正常に機能している点

### ✅ Stonecutter 設定
- 4バージョン（1.19.4, 1.20.1, 1.21.1, 1.21.11）の設定が完了
- 各バージョンの `gradle.properties` が適切に定義済み
- Fabric API バージョンは全て実在するバージョンで正確

### ✅ Gradle 設定
- Fabric Loom 1.13 を使用（最新かつ安定）
- 必要最小限の Fabric API モジュールのみ依存（軽量化）
- ビルド成果物の自動収集タスク (`buildAndCollect`) が実装済み

### ✅ Git 設定
- `.gitignore` が適切（build/, run/, IDE関連を除外）
- `.gitattributes` で改行コード制御が設定済み

### ✅ Mod メタデータ
- `fabric.mod.json` が Client Mod として正しく定義
- MIT ライセンスが明示
- GitHub リポジトリ情報が記載済み

---

## 📝 対応優先順位

### 【最優先】開発開始前に必須
1. ✅ Java 21 のインストールと設定（現在ビルド不可能）
2. ✅ `KuRadialMenuMixin.java` の修正または削除（実行時クラッシュの危険）
3. ✅ Access Widener の有効化または削除（不整合の解消）

### 【推奨】最初のコミット前に対応
4. ✅ `.editorconfig` の追加
5. ✅ 言語ファイルの作成（`en_us.json`, `ja_jp.json`）
6. ✅ CHANGELOG.md の初期化
7. ✅ README.md のプロジェクト固有内容への更新

### 【中期】開発中に整備
8. ✅ CI/CD パイプラインの構築
9. ✅ テストコード基盤の整備
10. ✅ 空ディレクトリの整理と `.gitkeep` 追加

---

## 🎯 次のアクション

### ✅ 実施済みの修正（2025年12月16日）

以下の修正をすべて完了しました:

1. ✅ **`.java-version` ファイル作成** - Java 21 を指定
2. ✅ **`KuRadialMenuMixin.java` の削除** - 誤った Server クラス注入を排除
3. ✅ **Access Widener の削除** - 未使用ファイルとビルド設定を削除
4. ✅ **`.editorconfig` の作成** - コーディングスタイルを統一
5. ✅ **言語ファイルの作成** - `en_us.json` と `ja_jp.json` を追加
6. ✅ **`CHANGELOG.md` の初期化** - Keep a Changelog 形式で作成
7. ✅ **`README.md` の更新** - プロジェクト固有の詳細な内容に差し替え
8. ✅ **空ディレクトリへの `.gitkeep` 追加** - 6つのディレクトリに配置

### 残りの手動対応事項

以下は手動で実行する必要があります:

#### 1. Java 21 のインストールと設定

```powershell
# Chocolatey を使用する場合
choco install temurin21

# または、手動でダウンロード
# https://adoptium.net/temurin/releases/?version=21

# インストール後、環境変数を設定
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.x.x-hotspot"
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", $env:JAVA_HOME, [System.EnvironmentVariableTarget]::User)
```

#### 2. IntelliJ IDEA の SDK 設定

1. **File > Project Structure > Project**
2. **SDK** を Java 21 に設定
3. **Language level** を "21 - Pattern matching for switch" に設定
4. **OK** をクリック

### 即座に実行すべきコマンド（Java 21 インストール後）

```powershell
# 1. Java バージョン確認
java -version  # "21.x.x" が表示されることを確認

# 2. プロジェクトのクリーンビルド
cd C:\projects\KuRadialMenu
.\gradlew.bat clean build

# 3. 開発環境の実行テスト
.\gradlew.bat runClient
```

### 📊 修正結果サマリー

| 項目 | 修正前 | 修正後 | ステータス |
|------|--------|--------|-----------|
| Java バージョン指定 | なし | `.java-version` (21) | ✅ 完了 |
| Mixin 設定 | Server クラス注入 | 削除 + client 専用化 | ✅ 完了 |
| Access Widener | 中途半端な状態 | 完全削除 | ✅ 完了 |
| エディタ設定 | なし | `.editorconfig` | ✅ 完了 |
| 言語ファイル | なし | en_us, ja_jp | ✅ 完了 |
| CHANGELOG | 空 | Keep a Changelog 形式 | ✅ 完了 |
| README | テンプレート | プロジェクト固有内容 | ✅ 完了 |
| ディレクトリ管理 | 空で未追跡 | `.gitkeep` 配置 | ✅ 完了 |

---

## 📚 参考資料

- [Fabric Wiki - Mixin Conventions](https://fabricmc.net/wiki/tutorial:mixin_introduction)
- [Stonecutter Documentation](https://stonecutter.kikugie.dev/wiki/)
- [Keep a Changelog](https://keepachangelog.com/)
- [Fabric Loom - Access Wideners](https://fabricmc.net/wiki/tutorial:accesswideners)

---

**レポート作成者**: GitHub Copilot  
**最終更新**: 2025年12月16日

